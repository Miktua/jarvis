import { readFileSync } from "node:fs";import { join } from "node:path";import { describe,expect,it } from "vitest";
const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260718073540_initial_jarvis_v1.sql"),"utf8").toLowerCase();
describe("database security migration",()=>{it("keeps user data outside exposed public schema",()=>{expect(sql).toContain("create schema if not exists user_data");expect(sql).toContain("revoke all on schema private, user_data from public, anon, authenticated")});it("enables RLS for all exposed metadata",()=>{for(const table of ["profiles","data_tables","data_table_permissions","workspace_nodes","bot_connections","messages","attachments","pending_actions","inbound_events","audit_log"])expect(sql).toContain(`alter table public.${table} enable row level security`)});it("creates separate data and schema capabilities",()=>{expect(sql).toContain("create role jarvis_data nologin noinherit");expect(sql).toContain("create role jarvis_schema nologin noinherit");expect(sql).toContain("grant create on schema user_data to jarvis_schema")});});
