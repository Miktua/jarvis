import { signIn,signUp } from "./actions";

export default async function AuthPage({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="auth-shell"><section className="auth-card"><div className="brand-mark">J</div><p className="eyebrow">PRIVATE DATABASE INTERFACE</p><h1>Welcome to Jarvis</h1><p className="muted">Sign in to browse your workspace or request private access.</p>{error&&<p className="error">{error}</p>}<div className="auth-grid"><form action={signIn}><h2>Sign in</h2><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" required minLength={8}/><button>Sign in</button></form><form action={signUp}><h2>Request access</h2><input name="displayName" placeholder="Display name" required/><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" required minLength={8}/><button className="secondary">Create account</button></form></div></section></main>}
