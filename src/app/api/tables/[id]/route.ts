import { NextResponse } from "next/server";import { requireApiActor } from "@/server/auth/session";import { requireTableAccess } from "@/server/auth/authorization";import { jsonError } from "@/server/http";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const actor=await requireApiActor();const t=await requireTableAccess(actor,(await params).id,"read");return NextResponse.json({displayName:t.display_name,schema:t.schema_definition,aiRules:t.ai_rules,access:t.owner?"owner":t.access});}catch(e){return jsonError(e);}}
