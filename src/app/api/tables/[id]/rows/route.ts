import { NextResponse } from "next/server";import { z } from "zod";import { requireApiActor } from "@/server/auth/session";import { searchRows } from "@/server/db-tools/table-service";import { jsonError } from "@/server/http";
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const url=new URL(request.url);const limit=z.coerce.number().int().min(1).max(100).default(50).parse(url.searchParams.get("limit")??50);const offset=z.coerce.number().int().min(0).max(10000).default(0).parse(url.searchParams.get("offset")??0);return NextResponse.json(await searchRows(await requireApiActor(),{tableId:(await params).id,filters:[],sort:[],limit,offset}));}catch(e){return jsonError(e);}}
