import { getConfig } from "@/server/config";
export async function telegram(method:string,body:Record<string,unknown>){const response=await fetch(`https://api.telegram.org/bot${getConfig().TELEGRAM_BOT_TOKEN}/${method}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body),signal:AbortSignal.timeout(10_000)});const data=await response.json();if(!response.ok||!data.ok)throw new Error(`Telegram ${method} failed`);return data.result;}
export async function sendTelegramMessage(chatId:number|string,text:string,replyMarkup?:unknown){return telegram("sendMessage",{chat_id:chatId,text:text.slice(0,4096),parse_mode:"HTML",...(replyMarkup?{reply_markup:replyMarkup}:{})});}
export async function answerCallbackQuery(id:string,text?:string){return telegram("answerCallbackQuery",{callback_query_id:id,...(text?{text}:{})});}
export async function getTelegramFile(fileId:string){const info=await telegram("getFile",{file_id:fileId}) as {file_path:string};const response=await fetch(`https://api.telegram.org/file/bot${getConfig().TELEGRAM_BOT_TOKEN}/${info.file_path}`,{signal:AbortSignal.timeout(20_000)});if(!response.ok)throw new Error("Telegram file download failed");return {bytes:new Uint8Array(await response.arrayBuffer()),path:info.file_path};}
